import * as auth from "./auth";
import * as courses from "./courses";
import * as lectures from "./lectures";
import * as enrollments from "./enrollments";

export const api = {
  auth,
  courses,
  lectures,
  enrollments,
};

export { auth, courses, lectures, enrollments };